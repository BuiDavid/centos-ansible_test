#!/bin/bash

# Install Nginx
echo "[Install Nginx] Installing Nginx"
sudo ansible-galaxy install geerlingguy.nginx

# Using playbook and yml to run role
sudo ansible-playbook geerling_nginx.yml

echo '[Install Nginx] done!'
